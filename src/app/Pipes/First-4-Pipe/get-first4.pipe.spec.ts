import { GetFirst4Pipe } from './get-first4.pipe';

describe('GetFirst4Pipe', () => {
  it('create an instance', () => {
    const pipe = new GetFirst4Pipe();
    expect(pipe).toBeTruthy();
  });
});
